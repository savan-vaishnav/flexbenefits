spring.application.name=claims-service-test

server.port=0

# H2 for tests
spring.datasource.url=jdbc:h2:mem:claimsdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

spring.flyway.enabled=false

jwt.secret=dGhpcyBpcyBhIHZlcnkgbG9uZyBzZWNyZXQga2V5IGZvciBmbGV4YmVuZWZpdHMgand0IHNpZ25pbmc=

benefits-service.url=http://localhost:8082

# Eureka - disable for tests
eureka.client.enabled=false
spring.cloud.discovery.enabled=false

# Kafka - disable for tests
spring.kafka.bootstrap-servers=localhost:0
spring.kafka.listener.auto-startup=false
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

# Redis/Cache - disable for tests
spring.cache.type=none
spring.data.redis.repositories.enabled=false

